<h1><%= t("event_list") %></h1>

<ul>
  <% @events.each do |event| %>
    <li>
      <%= link_to event.name, event_path(event) %> <%= event.created_at.strftime("%b %e %Y (%I:%M %p)") %>
    </li>
  <% end %>
</ul>
